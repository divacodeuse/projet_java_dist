package entity;

public interface IArticle {
    Article saisie();                  // Permet de saisir un article générique
    void affichage(Article a);         // Permet d'afficher les informations d'un article générique
}
